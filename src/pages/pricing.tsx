import AppLayout from '@/layouts/AppLayouts';
import { Box } from '@mui/material';

const Home = () => {
  return (
    <AppLayout>
      <Box>Pricing</Box>
    </AppLayout>
  );
};

export default Home;
